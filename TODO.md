1. В сваггере сделать examples под разные данные
2. Хот релоад аксепт добавить в нест (на изменения в env в дев режиме)

3. С сервера на клиент в сокетах при "from-server:error" можно отправлять код ошибки
4. POST /conversations разрешить всем авторизованным, а не только админам
5. Прикрутить пагинацию (скорее всего, cursor based)
6. В сокетах при "handleDisconnect" можно обновлять last seen date. Подумать еще об логике обновления
7. В сокетах при обновлении participants обновлять их в data.currentConversation у каждого client, который является участником чата. Наверное, через ивент бас слать оповещения о добавлении/удалении пользователя. А вообще, наверное, оставить только userIds этих participants, чтобы не морочиться с внутренним объектом User
8. Chat: разбить модуль на несколько разных доменов (+ мб переименовать conversation -> chat)

9. Добавить логгер
10. Редис для таблиц с типами

11. JWT авторизация нормальная с двумя токенами

12. Как мне валидировать content в бд таблице Message, чтобы туда невалидный объект не залетел? Только на беке?
13. deletedAt поля в бд + крон джобу на удаление через X дней
14. Внедрить кафку для какой-нибудь задачи (больше для изучения в рамках пет проекта, но мб обнаружу необходимый кейс)

Функционал будущий: 15. Разослать уведомления конкретному списку людей с каким-то текстом 16. Отложенные (и/или) регулярные автоматические сообщения/уведомления 17. Админ может заводить еще админов с полномочиями + настраивать приложение (менять название, иконку и раскраску цветовую)

18. npm проект аттачить в гит
19. https настроить
20. В докер завернуть корректно + делать бекапы (раз в X дней)

21. CI/CD:
<!-- Изменить флоу. Проверка всех линтеров + запуска в прод режиме -> обновление версии релиза -> публикация npm -->
22. И разбить шаги нормально на прогон тестов, на линтер и тд

23. Написать тесты на апишку
24. Организовать нормально моки (не через сервисы в seed файле, а сразу в бд лить данные)

25. ElasticSearch для поиска по БД
26. Шардирование

27. MessageLength указывать в MessageType (TEXT, PREMIUM_TEXT, IMAGE, ...) и ограничения в Message.content ставить такое, какое там указано
28. На будущее, Message.content {
    text: "message text @tag",
    mentioned: { <!-- Map(@tag - userId) + визуально вместо тега подставлять имя на фронте -->
    "@tag": "234"
    },
    repliedTo: "2" <!-- айдишник: сообщение, на которое ответили -->
    }
29. DB.Message.content: может делать null при type = SYSTEM_CONVERSATION_CREATED?

30. ESLint - включить отключенные правила
31. Обновить всё на новые версии + линтеры
32. Сортировка импортов наоборот от самых общих к самым специфичным (от зависимостей, потом шаред, потом уже модули, потом уже наши локальные файлы)
33. Добавить VS Code settings
34. Отдельно сделать директорию, которую буду заполнять контентом, который уйдет в npm пакет (мб внутри этой директории даже сделать какой-то конфиг, в котором я буду указывать откуда взять файл и куда положить [!!!])
35. Для Message таблицы можно MongoDB юзать из-за ее разной структуры

36. healthcheck БД перед сервисом
37. Добавить "map" полям и таблицам в БД и приводить к нижнему подчеркиванию
38. В ДТО на входах валидировать строки по min/max количествам символов + в БД если возможно, настроить ограничения (min, max). В моках поправить это тоже. А константы валидации добавлять и в npm-package для клиента
